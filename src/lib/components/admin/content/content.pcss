@tailwind base;
@tailwind components;
@tailwind utilities;

@layer components {
	.appbar {
		@apply sticky top-4 z-10 w-full bg-surface-50 p-4 rounded-container-token dark:bg-surface-700;
	}

	.appcontent {
		@apply mt-4 h-full;
	}
}
