@tailwind components;
@layer components {
	[data-button='refresh'] {
		@apply !variant-ghost-secondary btn  bg-error-400 p-1 rounded-container-token;
	}

	[data-button-icon='refresh'] {
		@apply p-1 text-secondary-500 rotate-45;
	}
}
